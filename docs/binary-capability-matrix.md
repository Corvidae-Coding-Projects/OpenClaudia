# Binary Capability Matrix — Registry Generation 4

Generated from `capabilities/registry.json` after typed evidence validation. Do not edit this table by hand: prose is not readiness evidence. `operational` requires reviewed executable success and failure receipts for every reachable entrypoint; other maturity labels retain their stated limitations.

| Capability | Invocation | Entrypoint | Reachability | Maturity | Required effects | Acceptance evidence | Limitation |
|---|---|---|---|---|---|---|---|
| Interactive TUI | `openclaudia` | default_tui | reachable | partial | network request, filesystem read, process execution, session mutation | none — not operational | Provider continuation, truthful terminal outcomes, run budgets, causal checkpoints, and runtime modes are wired; supervised cancellation and complete artifact-bound frontend evidence remain pending. |
| Interactive TUI | `openclaudia -m <model>` | default_tui | reachable | partial | network request, filesystem read, process execution, session mutation | none — not operational | Provider continuation, truthful terminal outcomes, run budgets, causal checkpoints, and runtime modes are wired; supervised cancellation and complete artifact-bound frontend evidence remain pending. |
| Interactive TUI | `openclaudia -v` | default_tui | reachable | partial | network request, filesystem read, process execution, session mutation | none — not operational | Provider continuation, truthful terminal outcomes, run budgets, causal checkpoints, and runtime modes are wired; supervised cancellation and complete artifact-bound frontend evidence remain pending. |
| Interactive TUI | `openclaudia --resume` | default_tui | reachable | partial | network request, filesystem read, process execution, session mutation | none — not operational | Provider continuation, truthful terminal outcomes, run budgets, causal checkpoints, and runtime modes are wired; supervised cancellation and complete artifact-bound frontend evidence remain pending. |
| Interactive TUI | `openclaudia --session-id <id>` | default_tui | reachable | partial | network request, filesystem read, process execution, session mutation | none — not operational | Provider continuation, truthful terminal outcomes, run budgets, causal checkpoints, and runtime modes are wired; supervised cancellation and complete artifact-bound frontend evidence remain pending. |
| Legacy line REPL | `openclaudia --tui-mode` | legacy_repl | reachable | partial | network request, process execution, session mutation | none — not operational | The route shares truthful terminal outcomes, budgets, checkpoints, and runtime modes, but request, hook, transcript, and cancellation semantics still drift from the default TUI. |
| Coordinator prompt mode | `openclaudia --coordinator --tui-mode` | legacy_repl | reachable | partial | session mutation, process execution | none — not operational | Coordinator child-run authority is enforced, but durable orchestration, cancellation, and artifact-bound lifecycle evidence remain incomplete. |
| Behavioral mode presets | `openclaudia --mode <preset>` | default_tui | reachable | partial | session mutation | none — not operational | Tool, effect, child-run, target, and in-flight background authority are enforced across production mode paths; artifact-bound VDD receipts remain pending. |
| One-shot print mode | `openclaudia --print "prompt"` | print | reachable | partial | network request, trace emission | none — not operational | Truthful terminal outcomes and aggregate budgets are wired; the route still lacks resumable canonical session state, supervised cancellation, and complete trace parity. |
| Project initialization | `openclaudia init` | init | reachable | partial | filesystem read, filesystem write | none — not operational | The multi-file overwrite path is not transactional and the scaffold still needs the remediated authority model. |
| Project initialization | `openclaudia init --force` | init | reachable | partial | filesystem read, filesystem write | none — not operational | The multi-file overwrite path is not transactional and the scaffold still needs the remediated authority model. |
| Legacy native authentication | `openclaudia auth` | auth | reachable | unsupported | network request, credential read, external mutation | none — not operational | Subscription-client impersonation is not approved for production and logout does not prove provider revocation. |
| Legacy native authentication | `openclaudia auth --status` | auth | reachable | unsupported | network request, credential read, external mutation | none — not operational | Subscription-client impersonation is not approved for production and logout does not prove provider revocation. |
| Legacy native authentication | `openclaudia auth --logout` | auth | reachable | unsupported | network request, credential read, external mutation | none — not operational | Subscription-client impersonation is not approved for production and logout does not prove provider revocation. |
| HTTP proxy | `openclaudia start` | proxy | reachable | unsupported | network request, credential read | none — not operational | Clients are unauthenticated, request and session state are not isolated, and lifecycle checks are incomplete. |
| HTTP proxy | `openclaudia start -p 9090` | proxy | reachable | unsupported | network request, credential read | none — not operational | Clients are unauthenticated, request and session state are not isolated, and lifecycle checks are incomplete. |
| HTTP proxy | `openclaudia start -t openai` | proxy | reachable | unsupported | network request, credential read | none — not operational | Clients are unauthenticated, request and session state are not isolated, and lifecycle checks are incomplete. |
| ACP stdio server | `openclaudia acp` | acp | reachable | partial | network request, process execution, session mutation | none — not operational | Typed partial outcomes, runtime modes, dynamic tools, budgets, and causal checkpoints are wired; session isolation, transport bounds, cancellation, and effective-capability negotiation remain incomplete. |
| ACP stdio server | `openclaudia acp -m <model>` | acp | reachable | partial | network request, process execution, session mutation | none — not operational | Typed partial outcomes, runtime modes, dynamic tools, budgets, and causal checkpoints are wired; session isolation, transport bounds, cancellation, and effective-capability negotiation remain incomplete. |
| Iteration loop | `openclaudia loop` | loop | reachable | partial | network request, session mutation, process execution | none — not operational | The loop consumes the canonical aggregate budget and truthful terminal outcomes, but still uses a separate orchestration lifecycle without durable stop/resume or cancellation parity. |
| Iteration loop | `openclaudia loop -n 10` | loop | reachable | partial | network request, session mutation, process execution | none — not operational | The loop consumes the canonical aggregate budget and truthful terminal outcomes, but still uses a separate orchestration lifecycle without durable stop/resume or cancellation parity. |
| Configuration display | `openclaudia config` | config | reachable | partial | filesystem read, credential read | none — not operational | Typed loading, precedence, unknown-field rejection, and secret redaction are wired; complete user-facing source provenance and conflict diagnostics remain incomplete. |
| Doctor diagnostics | `openclaudia doctor` | doctor | reachable | partial | filesystem read, credential read, network request | none — not operational | Standalone runtime checks remain unavailable, active provider reachability remains disabled until the canonical broker exists, and alternate-model VDD is pending S-088. |
| Doctor diagnostics | `openclaudia doctor --allow-active provider.reachability` | doctor | reachable | partial | filesystem read, credential read, network request | none — not operational | Standalone runtime checks remain unavailable, active provider reachability remains disabled until the canonical broker exists, and alternate-model VDD is pending S-088. |
| Repository hook proposals | `openclaudia hooks status` | hooks | reachable | partial | filesystem read, filesystem write, process execution | none — not operational | Per-invocation revocation, extension identity, executable binding, and complete lifecycle admission remain downstream work. |
| Repository hook proposals | `openclaudia hooks approve <sha256:...>` | hooks | reachable | partial | filesystem read, filesystem write, process execution | none — not operational | Per-invocation revocation, extension identity, executable binding, and complete lifecycle admission remain downstream work. |
| Repository hook proposals | `openclaudia hooks revoke <sha256:...>` | hooks | reachable | partial | filesystem read, filesystem write, process execution | none — not operational | Per-invocation revocation, extension identity, executable binding, and complete lifecycle admission remain downstream work. |
| Authenticated team technical memory | `openclaudia team <command>` | team_cli | reachable | partial | filesystem read, filesystem write, network request, credential read, external mutation | none — not operational | Authority, bounded replication, scoped tools, conflict handling, and production frontend activation are wired and deterministically tested; artifact-bound VDD receipts and non-Unix descriptor-safe private storage remain pending. |
| Authenticated team technical memory | `memory_save\|search\|list\|conflicts\|update\|delete (scope=team)` | agent_tool | reachable | partial | filesystem read, filesystem write, network request, credential read, external mutation | none — not operational | Authority, bounded replication, scoped tools, conflict handling, and production frontend activation are wired and deterministically tested; artifact-bound VDD receipts and non-Unix descriptor-safe private storage remain pending. |

The registry intentionally publishes `unsupported`, `experimental`, `schema-only`, `unreachable`, and `partial` states. Reachability proves only that an invocation reaches code; it does not promote maturity. The full audit remains the evidence source for limitations outside this initial registry.
